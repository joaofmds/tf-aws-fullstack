aws_region = "us-east-1"
org        = "exampleorg"
project    = "tf-aws-fullstack"
environment = "dev"

enable_kms               = true
create_access_log_bucket = true

# Optional. Defaults to envs/<environment>/terraform.tfstate
# state_key = "envs/dev/terraform.tfstate"

tags = {
  Owner       = "platform-team"
  CostCenter  = "1234"
  Terraform   = "true"
}
